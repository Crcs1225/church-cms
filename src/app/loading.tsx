import { LoadingScreen } from "@/components/ui";

export default function RootLoading() {
  return (
    <LoadingScreen
      title="Opening Workspace"
      description="Loading church records, settings, and the current admin session."
    />
  );
}
